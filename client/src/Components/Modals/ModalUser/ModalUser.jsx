import React from 'react';

import useStyles from './styles';

const ModalUser = ({ active, setActive }) => {
    const classes = useStyles();

    return (
        <div className={classes.overlay}>
            
        </div>
    )
}

export default ModalUser;
