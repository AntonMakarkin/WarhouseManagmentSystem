import { makeStyles } from "@material-ui/styles";

const useStyles = makeStyles((theme) => ({
    dataPageContainer: {
        paddingLeft: '20px',
        paddingRight: '20px'
    },
    dataItemsContainer: {
        
    }
}));

export default useStyles;