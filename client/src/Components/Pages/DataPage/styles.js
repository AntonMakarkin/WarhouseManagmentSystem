import { makeStyles } from "@material-ui/styles";

const useStyles = makeStyles((theme) => ({
    dataPageContainer: {
        paddingLeft: '20px',
        paddingRight: '20px'
    },
    addButton: {
        textTransform: 'none'
    },
    dataItemsContainer: {
        
    }
}));

export default useStyles;